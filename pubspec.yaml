name: idle_save
description: Thrilled to introduce a focused save SDK for idle games with deterministic serialization and explicit migrations.
version: 0.2.1
repository: https://github.com/tac0de/idle_save

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  clock: ^1.1.1
  crypto: ^3.0.3
  idle_core: ^0.3.1

dev_dependencies:
  lints: ^3.0.0
  test: ^1.25.0
